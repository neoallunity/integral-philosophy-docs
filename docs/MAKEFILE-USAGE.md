# Руководство по использованию Makefile

## Утверждение

Makefile обеспечивает автоматизацию всех рутинных операций при подготовке выпусков журнала.

## Основание

**[Проверено]** Make — стандартный инструмент автоматизации сборки с 1976 года (Stuart Feldman, Bell Labs). Декларативная природа Makefile соответствует принципу "что, а не как" (declarative vs imperative).

---

## Быстрый старт

### Основные команды

```bash
# Собрать PDF
make

# Очистить временные файлы
make clean

# Непрерывная компиляция (автоматическая пересборка при изменении)
make watch

# Открыть PDF
make view

# Показать все доступные команды
make help
```

---

## Категории команд

### 1. Сборка

| Команда | Описание | Когда использовать |
|---------|----------|-------------------|
| `make` | Стандартная сборка | Ежедневная работа |
| `make build` | То же, что `make` | Явная сборка |
| `make rebuild` | Полная пересборка | После изменения конфигурации |
| `make quick` | Быстрая компиляция | Проверка правки (без библиографии) |
| `make draft` | Режим черновика | Проверка вёрстки |

**Пример:**
```bash
# После добавления новой статьи:
make rebuild

# После правки текста:
make quick
```

### 2. Очистка

| Команда | Описание | Что удаляет |
|---------|----------|-------------|
| `make clean` | Стандартная очистка | Все временные файлы, кроме PDF |
| `make distclean` | Полная очистка | Включая PDF |
| `make clean-cache` | Очистка кэша | Только кэш latexmk |

**Пример:**
```bash
# Перед коммитом в Git:
make clean

# Перед архивированием:
make distclean
```

### 3. Непрерывная работа

| Команда | Описание | Применение |
|---------|----------|-----------|
| `make watch` | Автоматическая пересборка | Редактирование в реальном времени |

**Пример:**
```bash
# Запустить в отдельном терминале:
make watch

# Теперь каждое сохранение файла автоматически пересобирает PDF
```

**[Проверено]** `latexmk -pvc` отслеживает изменения всех зависимостей (chapters/*.tex, cfg/*.tex, references.bib).

### 4. Проверка качества

| Команда | Описание | Что проверяет |
|---------|----------|---------------|
| `make check` | Полная проверка | Ссылки + цитирования |
| `make check-refs` | Проверка ссылок | Undefined references |
| `make check-cites` | Проверка цитирований | Undefined citations |
| `make validate` | Проверка синтаксиса | Компиляция без ошибок |

**Пример:**
```bash
# Перед финальной публикацией:
make check

# Если обнаружены проблемы:
make report  # Подробный отчёт
```

### 5. Отладка

| Команда | Описание | Вывод |
|---------|----------|-------|
| `make warnings` | Предупреждения | LaTeX warnings из лога |
| `make errors` | Ошибки | LaTeX errors из лога |
| `make boxes` | Проблемы вёрстки | Overfull/Underfull hbox |
| `make report` | Полный отчёт | Всё вышеперечисленное |

**Пример:**
```bash
# После сборки появились warnings:
make warnings

# Проверить переполнения строк:
make boxes
```

### 6. Статистика

| Команда | Описание | Информация |
|---------|----------|-----------|
| `make stats` | Общая статистика | Страницы, слова, файлы |
| `make count-pages` | Количество страниц | Из PDF метаданных |
| `make count-words` | Количество слов | Приблизительно (требует detex) |
| `make info` | Информация о проекте | Файлы, дата сборки |

**Пример:**
```bash
# Узнать объём выпуска:
make stats

# Вывод:
# 📊 Статистика проекта:
#   Глав:        12
#   Конфигураций: 13
#   Размер PDF:  2.4M
```

### 7. Просмотр PDF

| Команда | Описание | Программа |
|---------|----------|-----------|
| `make view` | Системный просмотрщик | Автоопределение ОС |
| `make view-okular` | Открыть в Okular | Linux (KDE) |
| `make view-evince` | Открыть в Evince | Linux (GNOME) |
| `make view-zathura` | Открыть в Zathura | Linux (минимализм) |

**Пример:**
```bash
# После сборки:
make view

# Или специфичная программа:
make view-okular
```

**[Вывод]** `make view` автоматически определяет ОС:
- **macOS:** `open main.pdf`
- **Linux:** `xdg-open` → `evince` → `okular`
- **Windows:** `start main.pdf`

### 8. Архивирование

| Команда | Описание | Содержимое |
|---------|----------|-----------|
| `make archive` | Архив проекта | Исходники + конфигурация |
| `make archive-with-pdf` | Архив с PDF | Исходники + PDF |
| `make backup` | Резервная копия | Алиас для `archive` |

**Пример:**
```bash
# Перед крупными изменениями:
make backup

# Создан файл: main_20250614_143022.tar.gz
```

### 9. Git-интеграция

| Команда | Описание | Действие |
|---------|----------|---------|
| `make commit` | Коммит изменений | Интерактивный ввод сообщения |
| `make tag` | Создать тег версии | Для релизов |

**Пример:**
```bash
# После завершения работы над выпуском:
make commit
# Введите: "Выпуск №15 готов к публикации"

make tag
# Введите: v15.0
```

### 10. Специфичные для журнала

| Команда | Описание | Применение |
|---------|----------|-----------|
| `make update-metadata` | Обновить метаданные | Номер выпуска, год |
| `make new-article` | Создать новую статью | Из шаблона |

**Пример:**
```bash
# Начало работы над новым выпуском:
make update-metadata
# Номер выпуска: 16
# Год: 2025

# Добавление статьи:
make new-article
# Фамилия автора (латиница): ivanov
# Создан файл: chapters/article-ivanov.tex
```

---

## Типичные рабочие процессы

### Workflow 1: Ежедневная работа над выпуском

```bash
# 1. Открыть проект
cd journal-project

# 2. Запустить непрерывную компиляцию
make watch  # В отдельном терминале

# 3. Редактировать файлы в любом редакторе
# (PDF обновляется автоматически)

# 4. Проверить качество
make check

# 5. Закоммитить изменения
make commit
```

### Workflow 2: Подготовка к публикации

```bash
# 1. Полная пересборка
make rebuild

# 2. Проверка качества
make check
make report

# 3. Статистика
make stats

# 4. Просмотр финального PDF
make view

# 5. Создать архив
make archive-with-pdf

# 6. Тег версии
make tag
```

### Workflow 3: Начало нового выпуска

```bash
# 1. Обновить метаданные
make update-metadata

# 2. Очистить проект
make distclean

# 3. Добавить статьи
make new-article  # Для каждого автора

# 4. Первая сборка
make

# 5. Проверить
make check
```

### Workflow 4: Исправление ошибок

```bash
# 1. Собрать проект
make

# 2. Если есть ошибки:
make errors

# 3. Если есть предупреждения:
make warnings

# 4. Проверить вёрстку:
make boxes

# 5. Полный отчёт:
make report

# 6. Исправить и пересобрать:
make rebuild
```

---

## Настройка под себя

### Изменение движка компиляции

**Файл:** `Makefile`, строки 15-16

```makefile
# Текущее:
LATEX = lualatex

# Изменить на:
LATEX = pdflatex
# или
LATEX = xelatex
```

**[Не проверено]** При смене на `pdflatex` может потребоваться изменить `cfg/cfg-fonts.tex` (fontspec недоступен).

### Добавление новых расширений для очистки

**Файл:** `Makefile`, строки 33-35

```makefile
# Добавить расширение:
TEMP_EXTS = aux log toc out ... mw # <-- добавить mw
```

### Изменение цветов вывода

**Файл:** `Makefile`, строки 37-41

```makefile
# Отключить цвета:
NO_COLOR =
GREEN =
YELLOW =
RED =
BLUE =
```

### Добавление собственных команд

```makefile
# В конце файла добавить:

# Собрать только титульный лист
title-only:
    @echo "Компиляция титульного листа..."
    @$(LATEX) -interaction=nonstopmode chapters/00-titlepage.tex
    
.PHONY: title-only
```

---

## Интеграция с IDE

### Overleaf

**[Не проверено]** Overleaf не поддерживает Makefile напрямую, но можно использовать latexmkrc.

### VS Code

**Файл:** `.vscode/tasks.json`

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Make Build",
      "type": "shell",
      "command": "make",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "Make Clean",
      "type": "shell",
      "command": "make clean"
    },
    {
      "label": "Make Watch",
      "type": "shell",
      "command": "make watch"
    }
  ]
}
```

**Использование:** `Ctrl+Shift+B` → выбрать задачу

### TeXstudio

**Настройки → Сборка → Пользовательские команды:**

```
Команда: make
Аргументы: (пусто)
```

---

## Устранение проблем

### Проблема: "make: command not found"

**Решение:**

```bash
# Linux (Ubuntu/Debian):
sudo apt-get install build-essential

# macOS:
xcode-select --install

# Windows:
# Установите MinGW или используйте WSL
```

### Проблема: "latexmk: command not found"

**Решение:**

```bash
# Проверить зависимости:
make check-deps

# Установить (Ubuntu):
make install-deps-ubuntu
```

### Проблема: Непрерывная компиляция не запускается

**[Вывод]** Проверьте, что `latexmk` поддерживает флаг `-pvc`:

```bash
latexmk -help | grep pvc
```

Если нет, обновите latexmk:

```bash
tlmgr update latexmk
```

### Проблема: Очистка не удаляет файлы в chapters/

**Решение:** Проверьте права доступа:

```bash
ls -la chapters/
chmod u+w chapters/*.aux
make clean
```

---

## Оптимизация производительности

### Параллельная компиляция

**[Предположение]** Для больших проектов (50+ статей) можно использовать параллельную сборку:

```bash
make -j4  # 4 параллельных процесса
```

**Внимание:** LaTeX не всегда поддерживает параллельную компиляцию корректно.

### Кэширование

**[Проверено]** latexmk автоматически кэширует результаты. Для ускорения:

```bash
# Не очищать кэш между сборками:
make  # вместо make rebuild
```

### Быстрая проверка правок

```bash
# Только LaTeX, без библиографии/индексов:
make quick

# Или режим черновика:
make draft
```

---

## Заключение

**[Проверено]** Makefile обеспечивает:
1. ✅ Автоматизацию всех рутинных операций
2. ✅ Декларативное описание рабочего процесса
3. ✅ Переносимость между разработчиками
4. ✅ Самодокументирование (`make help`)

**[Предположение]** Использование Makefile сокращает время подготовки выпуска на ~40% за счёт:
- Устранения ручных операций
- Автоматизации проверок
- Единообразия процесса

**Критерий эффективности:** Новый сотрудник может начать работу, выполнив:

```bash
make check-deps  # Проверить зависимости
make             # Собрать проект
make help        # Узнать доступные команды
```

**Ограничения:**
- **[Не проверено]** Производительность на Windows может быть ниже
- **[Не проверено]** Требуется установленный Make (в Windows не по умолчанию)

**Авторитет:** Make — стандарт индустрии с 1976 года, используется в Linux kernel, LLVM, PostgreSQL и тысячах других проектов.
